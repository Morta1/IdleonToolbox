import { defineConfig } from '@playwright/test';

export default defineConfig({
  testDir: './e2e',
  // smoke-*.spec.js is auto-generated by generate-specs.js and gitignored. logged-out.spec.js is
  // hand-written and tracked, so it needs its own testMatch entry to run alongside them.
  testMatch: ['smoke-*.spec.js', 'logged-out.spec.js'],
  timeout: 30_000,
  expect: { timeout: 10_000 },
  retries: 0,
  workers: 4,
  reporter: 'list',
  use: {
    baseURL: 'http://localhost:3001',
    headless: true,
  },
  webServer: {
    command: 'npm run dev',
    url: 'http://localhost:3001',
    reuseExistingServer: true,
    timeout: 60_000,
  },
});
