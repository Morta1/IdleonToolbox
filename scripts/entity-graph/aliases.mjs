// Explicit rawName alias corrections. Map of wrongId -> correctId.
// Populated during Task 5 triage. Never add fuzzy logic here.
// Task 5 triage found no rawName drift: every craft material, drop target and quest
// reward/requirement that names a real item already matches an items.json key.
export const aliases = {};
